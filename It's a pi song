import re
t = int(raw_input())
pi = str(31415926535897932384626433833)
for i in range(t):
    n = raw_input()
    s = re.sub(r'[^A-Za-z0-9\s]+','',n)#substitute all special char other than whitespace to be '' - stripped
    l = s.split()
    y = ""
    for each in l:
        y = y + str(len(each))
    if pi[0:len(y)] == y:
        print "It's a pi song."
    else:
        print "It's not a pi song."
